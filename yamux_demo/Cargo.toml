[package]
name = "yamux_demo"
version = "0.1.0"
edition = "2024"

[dependencies]
yamux = "0.13"
tokio = { version = "1.45", features = ["full"] }
futures = "0.3"
tokio-util = { version = "0.7", features = ["compat"] }
